#include <iostream>
#include <bits/stdc++.h>

typedef long long ll; // shorthand for long long

using namespace std;
int main()
{
    // solution comes here

    // removes bottleneck of cin and cout
    ios::sync_with_stdio(0);
    cin.tie(0);

    // files
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);

    // TO comprare Floating point numbers
    float a, b;
    if (abs(a - b) < 1e-9)
    {
        // a and b are equal
    }
}