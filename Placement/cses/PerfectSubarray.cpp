#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int soln(vector<int> &arr, int N, int target)
{
}
int main()
{
    vector<int> arr = {1, 0, 1, 0, 1};
    int target = 2;
    cout << soln(arr, arr.size(), target);
}